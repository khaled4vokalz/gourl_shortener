server:
  host: localhost
  port: 8080
storage:
  type: in-memory
  # only applicable for other database options other than in-memory
  # dbConnString: "user=postgres password=<pass> dbname=gourl_shortener sslmode=disable"
cache:
  enabled: false
  # only valid for redis options
  # host: localhost
  # port: 6379
  # database: 0
  # username: <user>
  # password: <pass>
shortenerProps:
  length: 6
  maxAttempt: 5
